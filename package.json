{
  "name": "bitbot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node tools/dev-server.mjs",
    "build": "node tools/build-portal.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:ui": "playwright test",
    "check": "npm run build && npm run test && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
